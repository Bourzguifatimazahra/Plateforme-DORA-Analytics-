 name: Build & Test Next.js

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-test:
    name: Build & Test Next.js
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout code
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20
          cache: "pnpm"

      # 3️⃣ Installer pnpm globalement
      - name: Install pnpm
        run: npm install -g pnpm

      # 4️⃣ Installer les dépendances
      - name: Install dependencies
        run: pnpm install

      # 5️⃣ Linter le code
      - name: Run linter
        run: pnpm lint

      # 6️⃣ Lancer les tests unitaires
      - name: Run tests
        run: pnpm test

      # 7️⃣ Build Next.js
      - name: Build Next.js
        run: pnpm build

      # 8️⃣ Upload artefact pour déploiement
      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: next-build
          path: .next/
